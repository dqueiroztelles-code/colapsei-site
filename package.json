{
  "name": "colapsei-site",
  "version": "1.4.0",
  "private": true,
  "description": "Site do Colapsei. E Agora? com Mapa do Colapso e entrega protegida do livro.",
  "scripts": {
    "test": "node --test",
    "check": "node --check api/mapa.js && node --check api/book-checkout.js && node --check api/book-access.js && node --check api/book-download.js && node --check api/book-webhook.js && node --check lib/book.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "stripe": "22.4.0"
  }
}
